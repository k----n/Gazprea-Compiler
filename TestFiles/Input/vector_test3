procedure main() returns integer {
    integer vector x[4];
    integer vector y[4] = null;
    integer vector z[*] = [null, null, null, null];
    integer a[*] = [null, null, null, null];
    var out = std_output();


    x -> out;
    y -> out;
    z -> out;
    a -> out;

    return 0;
}