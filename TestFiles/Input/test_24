// loop tests

procedure main() returns integer{
    var out = std_output();
    var x = 3;
    loop {

	loop {
	  2 -> out;
	  if x > 0
	     x = x - 1;
	  else
	     break;
	  continue;
	  break;
	
	    loop {
	      3 -> out;
	      break;
	    }
	  }
	break;
    }


	return 0;
}
