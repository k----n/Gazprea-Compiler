/* Type promotion tests */

procedure main() returns integer{ 
        var out = std_output();

	integer x = 1; 
	real y = 1.3 + x;

	y -> out;

        /* must have a return */ 
        return 0; 
}
